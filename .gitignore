node_modules/
.DS_Store
*.log
.env
.env.local
.phpunit.result.cache
/vendor

# Internal planning doc — not part of the shipped engineering layer
VibeDiscipline-Full-Plan.md

# Derived eval fixture condition — regenerate per evals/README.md,
# never commit (would duplicate the whole .agent/ tree per trap)
evals/fixtures/*/vibediscipline/
